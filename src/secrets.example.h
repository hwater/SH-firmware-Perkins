#pragma once

// Template for src/secrets.h. Copy this file to secrets.h and set real values.
// secrets.h is gitignored so credentials never land in version control.
#define AP_SSID      "PERKINS"
#define AP_PASS      "changeme-ap-password"
#define OTA_PASSWORD "changeme-ota-password"
